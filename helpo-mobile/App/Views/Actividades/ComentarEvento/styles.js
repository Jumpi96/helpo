const React = require("react-native");

const { StyleSheet } = React;

export default {
  validationMessage: {
    color: '#ff0000',
    marginTop: 5,
    marginLeft: 20,
    marginBottom: 2.5,
  }
};

  